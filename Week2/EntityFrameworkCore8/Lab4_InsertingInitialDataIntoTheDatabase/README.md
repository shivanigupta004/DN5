# Lab 4: Inserting Initial Data into the Database

## Objective
Insert initial category and product data using EF Core.

## Example code
- Use `AddRangeAsync` to insert categories and products.
- Use `SaveChangesAsync` to persist the changes.

## Run
```powershell
dotnet run
```
