# Handson1 Output

Project path:
- `week_3/ASPNETCore8_WebAPI/Handson1`

## Verification

The API was started successfully with `dotnet run`.

### GET /api/values

Response:

```json
["Value1","Value2","Value3"]
```

### Notes

- The project is in a separate folder under `ASPNETCore8_WebAPI` as requested.
- The `ValuesController` supports read and write operations with standard Web API action verbs.
